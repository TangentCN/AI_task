# 基于CIFAR-10训练的图像识别网络

就是字面意思，是AI课的大作业。<del>也许也是用AI写的</del><br>
使用**pytorch**<br>
注意：由于源问题，torch没有被包含在toml文件里，仍需要本地独立下载。<br>
参考指令：`uv pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128`

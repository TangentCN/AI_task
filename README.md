# 基于CIFAR-10训练图像分类网络

人工智能基础大作业。
使用**uv**作为环境管理工具
安装uv后，使用`uv sync`以快速安装toml文件中的依赖
注意：由于源问题，torch和torchvison没有被包含在toml文件里，仍需要本地独立下载。
参考指令：`uv pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128`
